//to show datatypes and their ranges
#include <stdio.h>
void main()
{
    int a;
    char b;
    float c;
    double d;
    printf("The size of integer is: %d" ,sizeof(int));
    printf("\9nThe size of character is: %d", sizeof(char));
    printf("\nThe size of float is: %d" ,sizeof(float));
    printf("\nThe size of double is: %d" ,sizeof(double));
}
